mutation UpdateNote(
	$id: Int!
	$identifier: String!
	$title: String
	$body: String
) {
	updateNote(id: $id, identifier: $identifier, title: $title, body: $body) {
		id
		title
		body
		identifier
	}
}
