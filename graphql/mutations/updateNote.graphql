mutation UpdateNote(
	$id: Int!
	$identifier: String!
	$title: String
	$body: String
	$completed: Boolean
) {
	updateNote(
		id: $id
		identifier: $identifier
		title: $title
		body: $body
		completed: $completed
	) {
		id
		title
		identifier
		completed
		identifier
	}
}
